<div class="row">
  <div class="col-md-6 col-md-offset-3">
    {{#each model as |recipe|}}
      {{#recipe-list-item name=recipe.name}}

      {{/recipe-list-item}}
    {{/each}}
  </div>
</div>

{{outlet}}
