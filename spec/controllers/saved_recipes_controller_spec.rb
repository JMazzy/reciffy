require 'rails_helper'

describe SavedRecipesController, type: :controller do

  # let(:recipe){ create :recipe }

  # context 'user logged in' do
  #   login_user

  #   describe '#create' do
  #     it 'creates a saved recipe' do
  #       expect{ post :create, saved_recipe: { user_id: user.id, recipe_id: recipe.id } }.to change{ SavedRecipe.count }.by(1)
  #     end
  #   end
  # end

  # context 'user not logged in' do
  #   it 'does NOT create a saved recipe' do
  #     expect{ post :create, saved_recipe: { user_id: user.id, recipe_id: recipe.id } }.to change{ SavedRecipe.count }.by(0)
  #   end
  # end

end
